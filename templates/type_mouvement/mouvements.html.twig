{% extends 'base.html.twig' %}

{% block title %}Mouvement{% endblock %}
{% block monTitre %}Les différentes familles de mouvements{% endblock %}
{% block body %}
{% for type in types %}
<div class="row align-items-center">
    <div class="col-8 text-center">
        <h2 class="m-2">{{ type.libelle }}</h2> 
        <p class="ml-3 text-justify policePerso" >
            {{ type.description }}
        </p>
        <a href="{{path('afficher_type_mouvement', {'id': type.id})}}" class="btn btn-primary">Detail</a>
    </div>
    <div class="col-4 ">
        <img src="{{asset('images/type/' ~ type.image)}}" alt="">
    </div>
</div>
{% endfor %}
{% endblock %}
